# meta-ci selftest fixture

A valid `runtime-template` `repo-meta.yaml` used by `.gitea/workflows/meta-ci-selftest.yml`
to prove the reusable router runs and posts a single aggregate context on the live runner.
